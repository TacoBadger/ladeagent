[project]
name = "ladeagent"
version = "0.1.0"
description = "Ladeagent: MCP-server, agent og evals over danske elpriser (demo til DCC Energi)"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
include = ["ladeagent*"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
